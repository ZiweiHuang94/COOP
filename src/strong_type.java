/**
 * simple example shows that Java is a strong type language
 * the similar code work in c, which is a weak type language
 */
public class strong_type {
    public static void main(String[] argv){
        int i = (int) argv[0];
        System.out.println(i);
    }
}
